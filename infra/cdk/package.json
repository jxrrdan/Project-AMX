{
  "name": "@project-amx/infra-cdk",
  "version": "0.1.0",
  "private": true,
  "description": "AWS CDK infrastructure for AMS, mirroring the architecture in the Feature Specification v3.1. Structural scaffold — see README.md in this directory for what is and isn't wired up for a real deploy.",
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy --all"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "aws-cdk": "^2.1140.0",
    "aws-cdk-lib": "^2.268.0",
    "constructs": "^10.3.0",
    "source-map-support": "^0.5.21",
    "ts-node": "^10.9.1",
    "typescript": "~5.7.0"
  }
}
